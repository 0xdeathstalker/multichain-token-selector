export type Token = {
  id: string;
  chain: string;
  chain_id: string;
  amount: string;
  decimals: number;
  symbol: string;
  address: `0x${string}`;
  value_usd: number;
  price_usd: number;
};
